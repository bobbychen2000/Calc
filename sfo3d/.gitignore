# renders, videos, caches
out/
node_modules/
__pycache__/
*.pyc
# satellite screenshots (the user's Google Maps captures; reference only, shipped separately) and bulky intermediates
tools/sat/screens/
tools/sat/work/*.npy
tools/sat/work/*.png
tools/sat/work/*.jpg
tools/sat/work/uniq.txt
# Google-imagery crops (reference only, never redistributed)
docs/qa/ref/
# third-party downloads and recordings (licensed data stays local)
refs/
# 2-D sheets are regenerated by tools/drawing/run_all.sh (~30 MB per run); approved milestone sets are added with git add -f
docs/drawings/*.png
docs/drawings/*.svg
