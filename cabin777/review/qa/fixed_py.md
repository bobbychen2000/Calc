# PY fixes (items from other areas' from_*.md files)

No `from_*.md` file addressed an item to the PY area during this session. Everything below comes from the PY raters
(py_r3, py_w2a/b, py_w3a/b, py_w4a/b); the round-by-round list is in `py_report.md`.

- py_r3: every high and medium issue except q13 fixed (e7bb7d7). q13 (the camera is inside the partition) belongs to
  the integration area: see from_py.md.
